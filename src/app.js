import React from 'react';
import ReactDOM from 'react-dom';
import ViaCEPApp from './components/ViaCEPApp';

ReactDOM.render(<ViaCEPApp reason="Because still is in development process!" />, document.getElementById('root'));